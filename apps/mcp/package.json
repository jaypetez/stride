{
  "name": "@stride/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "stride-mcp": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "biome check ."
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "@stride/core": "workspace:*",
    "@stride/schemas": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  }
}
