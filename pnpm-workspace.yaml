packages:
  - "apps/*"
  - "packages/*"

# Single source of truth for versions shared across workspace packages.
# Reference from a package.json with "zod": "catalog:" etc.
catalog:
  zod: "^4.6.5"
  dotenv: "^18.0.1"
